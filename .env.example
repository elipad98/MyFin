# Database Connection (SQLite by default)
DATABASE_URL="file:./dev.db"

# Secret Key for JWT Sessions (change this to a secure random string)
JWT_SECRET="change-this-to-a-secure-secret-key"

# Port (Optional)
PORT=3000
